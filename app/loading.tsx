import { GlobalSpinner } from "./_components/global-spinner/global-spinner.component";

export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return <GlobalSpinner />
  }